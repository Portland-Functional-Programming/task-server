{
  "name": "task-scheduler-server",
  "version": "1.0.0",
  "description": "A web service for the Task Scheduler application.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tim McIver",
  "license": "ISC",
  "dependencies": {
    "uuid": "^8.3.1"
  }
}
